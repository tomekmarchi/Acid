var classTest = /^.[\w_-]+$/,
    tagTest = /^[A-Za-z]+$/,
    regexSpace = /\s/,
    regexSpaceglobal = /\s/g,
    regexDot = /\./g,
    regexDash = /-/g,
	regexToPath = /\.|\[/,
	regexOpenBracket= /]/g,
    regexFowardslash = /\//g,
    regexUnderscore = /_/g,
    isJSRegex = /\.js$/,
    isCSSRegex = /\.css$/,
    isJSONRegex = /\.json$/,
	hasDotRegex = /\./,
    rawURLDecodeRegex = /%(?![\da-f]{2})/gi,
    andRegex = /&/g,
    lessThanRegex = /</g,
    moreThanRegex = />/g,
    doubleQuoteRegex = /"/g,
	decimalCheck = /\.|\+/,
	fileExtension = /\.([0-9a-z]+)/,
    slashRegex = /\//g;
